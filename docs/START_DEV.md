# Как запустить n8n с кастомными нодами

## ✅ Решение (работает!)

### Шаг 1: Собрать проект

```bash
npm run build
```

###  Шаг 2: Установить пакет в n8n (один раз)

```powershell
cd $env:USERPROFILE\.n8n
npm install D:\repos\GNIVC\nodes\n8n-nodes-starter
```

Замените путь на ваш реальный путь к проекту.

### Шаг 3: Запуск n8n

```bash
npx n8n start
```

Или просто:

```bash
npm run dev
```

### Шаг 3: Откройте браузер

Откройте http://localhost:5678 и ваши ноды должны быть доступны:
- **Array Condition**
- **Array If-Then**
- **Header Condition**

---

## 🔄 При изменении кода

### Вариант 1: Ручная пересборка

1. Остановите n8n (Ctrl+C)
2. Соберите: `npm run build`
3. Запустите: `npm run dev`

### Вариант 2: Авто-пересборка (два терминала)

**Терминал 1 - watch mode:**
```bash
npm run dev:watch
```

**Терминал 2 - n8n:**
```bash
npx n8n start
```

При изменении `.ts` файлов код пересоберётся автоматически, но вам нужно будет:
- Перезагрузить страницу в браузере
- Или перезапустить workflow

---

## 🐛 Отладка

### С VS Code/Cursor

1. Поставьте `debugger;` в нужном месте кода
2. Соберите: `npm run build`
3. Запустите n8n с отладчиком:
   ```bash
   NODE_OPTIONS="--inspect=0.0.0.0:9229" npx n8n start
   ```
4. В VS Code/Cursor: **F5** → "Debug n8n Node (Attach)"
5. Выполните workflow — остановится на `debugger;`

### С console.log

В коде уже есть логи с префиксом `[DEBUG ArrayIfThen]`. Просто запустите `npm run dev` и смотрите терминал.

---

## ❌ Устранение проблем

### Ноды не отображаются

1. Проверьте линковку:
   ```bash
   npm list -g --depth=0 | findstr n8n-nodes-starter
   ```
   Должно показать: `n8n-nodes-starter@0.1.0 -> .\D:\repos\GNIVC\nodes\n8n-nodes-starter`

2. Если линковка не работает:
   ```bash
   npm unlink
   npm link
   ```

3. Проверьте, собран ли проект:
   ```bash
   Test-Path dist\nodes\ArrayIfThen\ArrayIfThen.node.js
   ```
   Должно вернуть `True`. Если `False`:
   ```bash
   npm run build
   ```

### n8n не запускается

1. Убейте все процессы node:
   ```powershell
   Get-Process -Name node -ErrorAction SilentlyContinue | Stop-Process -Force
   ```

2. Очистите кэш n8n:
   ```powershell
   Remove-Item -Path "$env:USERPROFILE\.n8n" -Recurse -Force -ErrorAction SilentlyContinue
   ```

3. Переустановите зависимости:
   ```bash
   npm install
   ```

4. Попробуйте снова:
   ```bash
   npm link
   npm run dev
   ```

---

## 📝 Команды

| Команда | Описание |
|---------|----------|
| `npm run build` | Собрать проект (один раз) |
| `npm run build:watch` | Собирать при изменениях (watch mode) |
| `npm run dev` | Собрать и запустить n8n |
| `npm run dev:watch` | Watch mode для сборки |
| `npm run lint` | Проверить код |
| `npm run lint:fix` | Исправить ошибки линтера |
| `npm link` | Создать глобальную ссылку на пакет |
| `npm unlink` | Удалить глобальную ссылку |
